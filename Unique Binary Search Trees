import java.util.*;

public class Solution {
	public int numTrees(int n) {
		if (n <= 2)
			return n;
		int[] dp = new int[n + 1];
		dp[0] = 1;
		dp[1] = 1;
		dp[2] = 2;
		for (int i = 3; i <= n; i++) {
			for (int a = 0; a < i; a++)
				dp[i] += dp[a] * dp[i - 1 - a];
		}

		return dp[n];
	}

}
